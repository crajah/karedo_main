# Default Config

mongo {
    server {
        host = "localhost"
        port = 27017
    }

    db.name = "wallet_data"
}

notification {
  email {
    auth.user.key = "6o9z6pfc93wq1koyhq1q0fn-g2wcur89"
    server.endpoint = "https://api.mailgun.net/v2/karedo.com/messages"
    sender = "noreply@karedo.com"
  }
  sms {
    auth {
      user = "user"
      pwd = "pwd"
    }
    server.endpoint = ""
  }
}

ui {
  web {
    server.address = "http://localhost:9000"
  }
}
