karedo_api is a POJO (Scala) API pack. 

It should be standalone usable in any services framework. 

It will contain the business logic and connectivity to the persistence layer.

karedo_persist will offer access to the persistence layer. It will also contain the model.



